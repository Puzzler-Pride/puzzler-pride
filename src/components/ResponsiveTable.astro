---
// src/components/ResponsiveTable.astro
const { breakpoint, className, ratio, textAlign, zeroPadding } = Astro.props
---
<table-saw
  breakpoint={breakpoint} 
  class={className} 
  ratio={ratio} 
  text-align={textAlign}
  zero-padding={zeroPadding}
>
  <slot />
</table-saw>
  
<script>
  import '@zachleat/table-saw';
</script>
